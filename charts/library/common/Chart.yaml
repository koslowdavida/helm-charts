---
apiVersion: v2
name: common
description: Function library for Helm charts
type: library
version: 4.1.1
kubeVersion: ">=1.28.0-0"
keywords:
  - common
  - library
home: https://github.com/koslowdavida/helm-charts/tree/main/charts/library/common
maintainers:
  - name: koslowdavida
    email: koslowdavida@gmail.com
sources:
  - https://github.com/koslowdavida/helm-charts
annotations:
  artifacthub.io/changes: |-
    - kind: fixed
      description: |-
        Fixed an edge-case bug where specifying a merge defaultPodOptionsStrategy could crash the chart.
    - kind: fixed
      description: |-
        Fixed Route automatic service detection if there are no rules defined.
